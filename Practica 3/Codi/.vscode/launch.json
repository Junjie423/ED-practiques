{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug C++ (CodeLLDB)",
            "type": "lldb",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "args": [],
            "cwd": "${fileDirname}",
            "terminal": "integrated", 
            "preLaunchTask": "C/C++: Compilar archivo activo"
        }
    ]
}